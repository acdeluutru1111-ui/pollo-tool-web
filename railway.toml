[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --worker-class gevent --worker-connections 200 --timeout 300"
healthcheckPath = "/"
restartPolicyType = "on_failure"
